int y;

int gcd(int u, int v) {
  if (v == 0) return u;
  else {
    output(u);
    output(v);
    return gcd(v, u - u/v*v);
  }
}

void main(void) {
  int x;
  x = input();
  y = 10;
  output(gcd(x, y));
}
